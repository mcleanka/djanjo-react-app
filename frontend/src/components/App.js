import React, { Component } from 'react';
import ReactDOM, { render } from 'react-dom';

ReactDOM.render(
	<React.StrictMode>
		<div>
			Hello django
		</div>
	</React.StrictMode>,
	document.getElementById('app')
);